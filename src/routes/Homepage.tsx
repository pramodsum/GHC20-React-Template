import React from "react";
import "typeface-roboto";

import Layout from "../components/Layout";

const Homepage: React.FC = () => {
  return <Layout>This has a list of stuff</Layout>;
};

export default Homepage;
